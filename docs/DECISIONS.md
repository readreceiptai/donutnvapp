# Decisions Log

Why things are the way they are. Newest first. Update this whenever a non-obvious call is made.

## 2026-08-25

- **#57 — customer ownership policy + re-signup clobber fix (`complete_signup`).** A re-signup reuses the same auth identity (OTP on an existing email), and the old RPC did `on conflict (id) do update set tenant_id = excluded.tenant_id, ...`. So a returning customer who signed up again through another territory's app had their `profiles.tenant_id` flipped — and since operator RLS/roster is `is_operator() AND tenant_id = current_tenant_id()`, the original franchise **lost the customer entirely**. It also overwrote contact fields with blanks and appended duplicate consent + Home-area rows each time. Worsens with volume.
  - **Policy decided — FIRST-TOUCH OWNERSHIP.** `profiles.tenant_id` (the field RLS keys on) is the customer's home franchise, set once at first signup and **never** moved by a re-signup. Cross-territory service is handled per-booking via `owner_tenant_id` / `route_booking`, not by changing the home tenant. A re-signup is a returning-customer event: fills/refreshes contact fields from non-empty values only (blank never wipes), preserves loyalty (`points_*`, `referral_code`, `referred_by`), role, superadmin, parent linkage and `created_at`; keeps a single `Home` area; records a consent row only when the value actually changed.
  - **Re-tested live (rolled back), same method as the security fixes:** signup in tenant A (Palm Harbor) + accrued points/referral/`owner_tenant_id`, then re-signup through tenant B (Harrisburg) with blank last/phone/email + marketing opt-out → `tenant_id` stayed A, `owner_tenant` stayed A, no field wiped, points 500/500 + referral intact, single Home row (zip updated to new value), opt-out recorded as the latest consent. A separate identical re-signup added **zero** consent/area rows (no audit bloat). No frontend change needed. Migration `20260825_fix_57_complete_signup_first_touch`.

- **#122 / M1 — server-side bot enforcement for bookings (edge-wrapper).** Decided: close the anon `submit_booking` hole before beta via an edge-wrapper; defer native Auth CAPTCHA for OTP to land with the SMS/10DLC launch (#35), not open-ended. Built `submit-booking` edge function (verifies caller JWT + Turnstile token, then calls `submit_booking` as service_role with the verified `p_created_by`); `submit_booking` revoked from anon/authenticated (service_role-only). BookTruck now calls the edge fn instead of the RPC. Re-tested (rolled back): anon/authenticated EXECUTE=false, service_role path creates+routes with correct `created_by`; live smoke test anon POST → 401. **Follow-up tracked for #35:** enable native Turnstile/hCaptcha in Supabase Auth (Dashboard → Auth → Bot & Abuse) and pass `options.captchaToken` on `signInWithOtp` in SignUp/Login.

- **#122 — Medium/Low security sweep (non-gating).** Fixed + re-tested live (rolled back): **M2** `get_event_tracking` now expires ~2 days post-event and won't resolve cancelled bookings (was leaking event PII to any token holder forever); **Low** `wallet_passes.auth_token` no longer client-readable (revoked table SELECT, re-granted all columns except the token; service_role bypasses so pass-push still works). Verified already-handled: **M7** trigger order is moot post-C1 (both triggers identity-checked); **M8** Maps keys already locked (web referrer-locked, native API+quota-scoped) — added `loading=async`; **Low** Sentry already bundled. **Deferred, needs decision — M1** server-side bot enforcement: Turnstile is verified (`verify-turnstile`) but decoupled from the action (`submit_booking` anon-callable, `signInWithOtp` direct), so a bot can bypass it; fix is a booking edge-wrapper + native Supabase Auth CAPTCHA, which changes the public booking/auth path. Reliability items M3–M6 are non-security, deferred. Migrations `20260825_fix_122_get_event_tracking_expiry`, `20260825_fix_122_wallet_passes_hide_auth_token`.

- **Beta-gate security re-verification (REVIEW_FINDINGS.md).** Re-tested every Critical + High against LIVE main (role-simulated SQL, rolled back; live grant checks; endpoint checks). Two were NOT actually closed and are now fixed:
  - **C1 (Critical) — was partially open.** `auto_superadmin` correctly used verified `auth.email()`, but the superadmin guard fired only on UPDATE while `authenticated` holds a table-level INSERT grant (covers all columns). Proven live: an authenticated user could INSERT their own profile with `is_superadmin=true`. Fixed by making `profiles_superadmin_guard` fire on INSERT OR UPDATE (verified-owner / existing-superadmin only). Re-tested: attacker insert/update → false, verified owner → true.
  - **H1 (High) — was still open.** `route_booking(uuid)` had EXECUTE to PUBLIC on live (a CREATE OR REPLACE re-granted the default). Fixed by revoking from public/anon/authenticated; re-tested anon direct call denied + `submit_booking` still routes.
  - **C2** hardened: added a prod-domain guard to `previewEnabled()` (donutnvapp.com can never preview even if the env var leaks) + confirmed `VITE_PREVIEW_MODE` absent from all Netlify contexts + `/admin`,`/admin/customers`,`/elle` redirect to landing in the prod bundle.
  - **H5** ZIP/phone validation added to SignUp (`/^\d{5}$/`, 10–11 digits); parent email already persisted via complete_signup.
  - **H2, H3, H4, H6, H7** verified already fixed (H3 re-tested underpay→pending/correct→paid; H2 operator+tenant check; H6 atomic complete_signup; H4 loading re-entry + NeedsSetup; H7 no /welcome). Full status matrix in REVIEW_FINDINGS.md.

- **Backend-unreachable health check (#121)** — customer-facing complement to the UptimeRobot ops monitor. New `BackendHealthGate` (wraps `<App/>` in main.jsx, inside BetaGate) pings `${SUPABASE_URL}/auth/v1/health` (apikey only, lightweight) at boot + on an interval; if unreachable it renders the shared branded `OutageScreen` instead of a hung/blank app, and auto-recovers (no reload) once Supabase answers. Fail-safe against false blackouts: `checkBackend()` retries (2 attempts, 4s timeout each) before ever flipping to down, "reachable" = any response < 500 (a 4xx still means Supabase answered), and the app renders normally until a check CONCLUDES down. Polls 30s while up / 8s while down (fast recovery); an `online` event forces a re-check. Extracted `OutageScreen` so ErrorBoundary (render crashes) and the health gate share one outage UI. Verified against a simulated outage (fetch monkeypatch): boot-up → app; outage → branded screen; recovery → auto-restored on the interval with no reload.

- **Branded status assets wired in** (`status-pages/`). (1) `outage.html` design ported into the top-level `ErrorBoundary` (render-crash fallback) and shipped as a static `/outage.html`; (2) `maintenance.html` shipped as `/maintenance.html` and served by the edge gate when `MAINTENANCE_MODE` env is `on/true/1` (503; overrides the password gate + onboarding; toggle = set the Netlify env var + redeploy the edge fn); (3) `inline-error.html` became a reusable `<InlineError onRetry busy>` component, wired into the failed-submit path of Onboard, BookTruck, and Fundraise — it preserves the user's input (form state is untouched on error) and the button re-runs the submit. Gate now also exempts `/outage.html`, `/maintenance.html`, `/mini_donut.png` so the status pages render ungated. **Fix applied:** the assets put `logo-white.png` on a white card (invisible) — switched to `logo-black.png` + root-relative image paths so they work on any host. (4) `back-online-email.html` wired as an **ops-only recovery alert** (Kevin's call — no mass customer email until affected-user tracking + consent exist): new `back-online-notify` edge fn (`verify_jwt=false`, `?key=` shared secret, config in `app_config`) receives an UptimeRobot recovery webhook and emails the branded back-online template via Resend to `kevindmc@trenchlogic.com` — but ONLY on recovery (alertType=2); down alerts (type=1) send nothing. Deployed + tested end-to-end (wrong key→401, down→no email, recovery→delivered from `status@send.donutnvapp.com`). Kevin still needs to add the UptimeRobot "up" webhook URL pointing at the fn with `?key=…&type=*alertType*&monitor=*monitorFriendlyName*`.

- **Onboarding entry + email notification.** (1) A "Ready to Onboard — Start Here" CTA added to the owner/franchisee login (`OwnerLogin.jsx`), a plain `<a href="/onboard">` so it loads the STANDALONE public form (not the in-SPA route). Deliberately NOT on the customer landing — franchisee onboarding shouldn't show to donut customers. It's behind the site gate until launch (expected). (2) New `onboarding-notify` edge function emails each `onboarding_intake` INSERT to `kevindmc@trenchlogic.com` via Resend (`send.donutnvapp.com`), formatting labeled fields (not raw JSON) + the row id. Notification is a layer on top; the table stays system-of-record. Fail-safe: it's a Database Webhook (fires AFTER commit) and the function never throws / always 200s, and the trigger uses `net.http_post` (fire-and-forget) wrapped in an exception guard — so a broken/unconfigured notifier can never block a submission. **Config is read from `app_config` (RLS on, no policies = service-role only), NOT Deno.env** — because Edge Function secrets can't be set from this environment (no CLI auth / no MCP secrets tool), and the function already has the service role. Keys: `onboarding_notify_url`, `onboarding_notify_secret` (shared `?key=`), `resend_api_key` (+ optional `onboarding_notify_from`/`_to`). **ACTIVATED 2026-08-25**: function deployed (`verify_jwt=false`), migration applied, all three `app_config` values set, and a real test submission verified end-to-end — trigger→function 200 and Resend accepted with a message id. **Still Kevin's (dashboard-only):** #34 Auth custom SMTP (Authentication → Emails → SMTP) with the same Resend key + `{{ .Token }}` templates — no MCP/CLI path for Auth config, so it can't be done from here.

- **MotoWatchdog puck tracking → same pipeline as any puck.** New `motowatchdog-webhook` edge function (APP project, `verify_jwt=false`, shared `?key=MW_WEBHOOK_SECRET`, fail-closed). The handoff doc's starter upserted `truck_latest_location`, but that is a VIEW — so the receiver instead mirrors `puck-ingest`: resolve device → truck, keep a rolling PUBLIC `live_session` open, insert a `truck_locations` row (the map's `truck_latest_location`/`active_live_sessions` views read from there). Registry is a `trucks.motowatchdog_external_id` column (not a new `device_registry` table) because `truck_locations`/`live_sessions` already key off `trucks.id`/`tenant_id`; set each puck's `external_id` to that value so every webhook is self-identifying. `live_sessions.source` CHECK only allows `manual|schedule`, so sessions are tagged `manual` (not `motowatchdog`). Speed/address (no columns on the hot `truck_locations` table) are kept in the raw `motowatchdog_webhook_log` (RLS on, no policies = service-role only) until a UI needs them. **Pending Kevin (external):** set `MW_WEBHOOK_SECRET` in APP secrets (fn is fail-closed until then), register the test truck's `motowatchdog_external_id`, and set the MW webhook URL + event subscription. His `MOTOWATCHDOG_AUTH_TOKEN`/`PARTNER` keys are only needed for device registration via API + the REST-pull fallback (not built yet — documented insurance/backup that reuses this write path).

- **Branch consolidation: `main` is the single source of truth; prod deploys from `main` only.** Merged `feature/maps-cost-rewards-polish` (which was stacked on and subsumed `wallet-referral-rewards` + `feature/proximity-push`) into `main` alongside `/onboard`. Deleted all feature branches + a stale `main.lock.x669420540` ref + 34 stale `.git` lock files + 38 iCloud " 2" conflict copies. Proximity rides along **dormant** (kill switch OFF, web stub hidden) — its activation stays gated on the real-device test, and no proximity SQL/edge functions were applied to Supabase. android/ios strip is a planned later pass.
- **Postmortem — preview-mode leak + missing gate on prod (found + fixed).** The 08-24 `/onboard` deploy was built from a worktree that had `.env` (which carries `VITE_PREVIEW_MODE=1` for local dev) but NOT `.env.production` (which blanks it), so prod shipped with **preview mode ON — the whole operator/ELLE app was reachable at `/admin` with no login** (data still RLS-protected, but the login gate was bypassed). That deploy also shipped from a tree lacking `netlify/edge-functions/gate.ts`, so the site password wall was gone too. Fix: always deploy from a checkout that has `.env.production` (the main checkout), and **`gate.ts` is now tracked on `main`** (it was living only as an untracked working-tree file — a clean deploy would have shipped with no gate at all). Lesson: never deploy prod from a worktree missing `.env.production`.
- **`.gitignore`: `dist-*` and `.env.*` (keep `.env.example`).** Build dirs and `.env.production` (secrets) must never be committed.
- **`/onboard` is a STANDALONE page, gate-exempt.** To let invitees reach the intake form without the site password AND without unlocking the beta app, `/onboard` is its own Vite entry (`onboard.html` + `src/onboard-entry.jsx`) that renders only the wizard — no router, no app shell. The edge gate exempts `/onboard`, `/onboard.html`, and `/assets/*`; the main app's `index.html` stays gated, so the app is not runnable from any exempt path (the served `/assets` JS is inert minified code, already shipped to testers). Calendly link is `kevin-donutnv/30min`. Turnstile (#60) is wired on the form (required on the final step + server verify) but **dormant until `VITE_TURNSTILE_SITE_KEY` (Netlify) + `TURNSTILE_SECRET_KEY` (Supabase) are set**, then it enforces.

## 2026-08-18

- **App identifier is `com.donutnv.app` on both platforms, no rename.** Kevin's call. It reads right, it is the public Play Store URL, and for any white-label future each client gets its own brand-named ID, so a `com.trenchlogic.*` prefix would be wrong here. Verified identical in `capacitor.config.json`, Android `applicationId`/`namespace`, the compiled APK, and iOS `PRODUCT_BUNDLE_IDENTIFIER`. Fallback: if the App ID is taken in the Apple team, Kevin supplies the exact replacement and it is renamed in one pass **before** any store listing exists.
- **Repo stays in `~/Desktop/Builds/donutnvapp` until after the franchisee demo.** iCloud Desktop sync minted ` 2` conflict copies of ~23 files during the Capacitor scaffold and one (`config 2.xml`) broke the Android resource merge. The permanent fix is moving out of `~/Desktop`, but Kevin will do that himself post-demo, coordinated with the other Cowork session and the Netlify deploy directory. Until then: sweep conflict copies on sight. Demo is unaffected (runs off the deployed site).
- **Merge gate for `feature/proximity-push`: a real push landing on a real phone.** Not before. Kill switch stays `false` throughout.

## 2026-08-16

- **Option B (live proximity push) server-side pipeline built** on branch `feature/proximity-push`. Full detail in `docs/PROXIMITY-PUSH.md`. The non-obvious calls:
  - **Two position tables, not one.** `customer_positions` is 24h history; `customer_latest_position` is one row per customer and is the only thing `ST_DWithin` touches. Matching against history would mean scanning millions of rows to answer a question about ~100K people. The GiST index lives only on the hot table, so the high-write history table is not paying for an index nobody queries.
  - **Customer location is readable by the customer and nobody else.** Not operators, not superadmin. A franchisee has no business reading where their customers physically are, and that restraint is what makes the App Review background-location story defensible. RLS is `enable` **and** `force`; `anon` has no grant on any of the six new tables. Debugging goes through service role, which is audited.
  - **24h retention for customer positions vs 48h for `truck_locations`.** A truck's breadcrumb trail is business data; a customer's is PII. Different thing, different window.
  - **New `tenant_proximity_config` table instead of columns on `tenants`.** Keeps the isolation rule absolute: this workstream created objects and altered nothing.
  - **Reused the existing `proximity_pushes (session_id, profile_id)` interlock** rather than inventing a new dedupe. It means legacy `notify-proximity` and new `proximity-dispatch` can run side by side during cutover without double-notifying anyone.
  - **All rules live in `match_proximity_candidates`, in SQL.** Kill switch, tenant toggle, opt-in, freshness, radius cap, quiet hours, frequency + daily caps, dedupe. One place to audit, and the caps cannot be bypassed by a buggy client or a second dispatcher.
  - **Daily cap is a rolling 24h window, not a calendar day** — a calendar cap is gamed by a truck going live at 11:55pm.
  - **Opt-in is two-step** (our priming screen, then the OS prompt). The OS "Allow all the time" dialog can only be spent once and a Deny is effectively permanent, so it is only spent on customers who already said yes to us. Opt-in rate is the metric the whole feature lives on.
  - **Location plugin sits behind a provider interface.** Free `@capacitor-community/...` is active now to prove the pipeline; the paid Transistorsoft provider is written against the real API but inert. Its import is hidden from the bundler (`@vite-ignore` + variable specifier) so an uninstalled paid package cannot break `vite build`. Kevin's call: buy and swap before beta, because reliability is the moat.
  - **Kill switch ships OFF** (`app_config.proximity_push_enabled = 'false'`), tenant toggles default off. Nothing sends until deliberately switched on.
  - **CTR via a narrow RPC, not an UPDATE grant.** Granting customers UPDATE on `proximity_notification_log` would let them rewrite status/title/distance — the metrics themselves. `mark_proximity_notification_opened()` sets one column, on their own rows, once.
  - **Doc correction:** `docs/DATA-MODEL.md` claimed `profiles.id` has **no** FK to `auth.users`. It does (`profiles_id_fkey`) — found when a test insert failed. Corrected.
  - **Brand note:** legacy `notify-proximity` push copy uses the donut emoji, which `CLAUDE.md` bans. The new dispatcher's copy does not. Legacy copy left alone to avoid touching another session's surface; worth fixing when it is retired.

## 2026-08-14

- **Pre-launch review + fix pass** (full-codebase review, then a validated fix script). Landed:
  - **platform-metrics ELLE leak (P0):** the ELLE metrics block returned every franchise's leads/won/lost/booked_revenue/pipeline to ANY logged-in user. Now superadmin-only for the full network; a non-super franchisee gets only their own ELLE tenant (resolved by login email).
  - **elle-dashboard paid-spend gate (P0):** `find_linkedin`/`find_press` now check `paid_apis_enabled` AND that the `business_id` is on the caller's own board before spending Apify/Apollo — the confirm switch is now enforced at this direct path, not just in the SQL orchestrators.
  - **Login account-creation trap (P0):** Login now passes `shouldCreateUser:false` and shows a "create one" link on unknown email, so a mistyped login can't spin up a profile-less auth user. Added a `NeedsSetup` recovery screen for the session-without-profile state (was an infinite spinner).
  - **BetaGate mounted (P0):** the private-beta password wall was dead code; now wired in `main.jsx` (no-op unless `VITE_BETA_PASSWORD` is set). Docs and code now agree.
  - **ErrorBoundary + bundled Sentry (P1):** top-level React error boundary (branded reload card, reports to Sentry) so one bad render can't white-screen the app. Sentry is now the bundled `@sentry/react` (added to package.json/lock), not a runtime CDN import.
  - **elle-onboard authorization (P1):** self-provisioning an ELLE tenant + claiming ZIPs now requires operator/superadmin, not just any signed-in email.
  - **Repo rebuild-safety (P1):** removed the regressing/insecure snapshot definitions so a from-scratch `psql -f schema_*.sql` can't undo live fixes — `auto_superadmin` now uses verified `auth.email()` in schema_superadmin.sql, `route_booking` is revoked (not re-granted) in schema_territory.sql, `get_corporate_metrics` uses `is_superadmin()` in schema_corporate.sql, `camp_read` is tenant-scoped in schema.sql. Live DB stays the authoritative rebuild source until a real ordered-migration manifest exists (#62).
  - **Private live-location RLS (item 10):** new migration `20260814_scope_live_location_public_reads.sql` (APP project) scopes `live_read`/`loc_read` off `using(true)` so private "on the way" sessions + en-route GPS aren't anon-readable. Base files (schema.sql visibility-free interim + schema_bookings.sql authoritative) kept in sync. **Must verify the customer Find map still shows a live public truck after applying.**
  - **Dispatcher deny-by-default (P2):** new migration `20260814_elle_dispatcher_deny_by_default.sql` (ELLE project) blocks any unclassified function instead of dispatching it ungoverned. No behavior change today (all dispatched names are already mapped paid functions).
  - **P2 cleanup:** map centers on the tenant's centroid (not Palm Harbor FL); `?testpin=1` and `/track/demo` demo backdoors gated to preview builds only; `seed.sql` guarded against running on prod; consent toggle reverts + warns on a failed write; double-submit ref-guards on Signup/Book/Fundraise; puck-ingest rejects out-of-range/`0,0` coordinates; elle-apollo-webhook accepts the secret via header (query kept for Apollo).
  - **Left intentionally:** `demo-checkin` key stays in the URL (it's a clickable demo link). It is intentionally ON prod; the safety control is the `app_config` key + email allowlist + goal cap, not absence from prod. Do NOT delete it — that breaks the live demo.
  - **Still on Kevin (config, unchanged by this pass):** confirm `VITE_PREVIEW_MODE` is deleted on the prod Netlify site; restrict the Google Maps browser key by referrer; set Turnstile keys before public OTP/booking.

## 2026-08-13

- **Post-build audit** (Supabase security advisors + state checks) — clean overall; fixed 3 issues we'd introduced: enabled RLS on new tables `elle_example_seed` + `elle_onboarding` (were exposed via REST), revoked anon/authenticated execute on the `elle_tenants_seed_trg` / `elle_tenants_confirm_trg` trigger functions, and pinned `elle_enforce_event_zip` search_path. Verified: 0 tenants paid-enabled, 0 test residue, all objects present, APP RPCs not anon-callable. Pre-existing advisor items (service-role tables with RLS-deny, pg_net in public, leaked-password protection = ROADMAP #55-area) left as-is.
- **One-button onboarding backbone (#48)** built: `elle_provision_tenant` runs free stages always, gates paid stages on `paid_apis_enabled`, tracks progress in `elle_onboarding`. Closed a spend gap — `elle_onboard_territory` previously spent regardless of confirm; now gated. Confirming a Z auto-clears examples + triggers the paid cold-load. Remaining: audit/verify stage (#49), UI button.
- **Cost dashboard (#58 half)** shipped: `elle_cost_dashboard` / `elle_cost_by_service_phase` split actual vs estimate per client. Precise per-function actual logging deferred (needs coordinated 6-function + dispatcher change to avoid double-counting; no live spend yet since no Z confirmed). #58 stays open for that.
- **#61 Rewards tenant filter** shipped to prod (defense-in-depth on RLS).


- **Franchisor pays → no per-Z billing gate.** Corporate is expected to fund the platform, so we are NOT building per-franchisee payment/billing. The `paid_apis_enabled` confirm switch stays (it's the "this Z is live, start spending" control) but nothing is invested toward a per-Z-pays scenario. Could change; don't remove anything.
- **Beta rollout preference: contiguous regional cluster.** Preferred order: (1) contiguous regional cluster, (2) single-state saturation, (3) representative archetype sample. Reason: only contiguous coverage lets us test/prove Alex's "out-of-bounds lead → nearest Z + fair distribution" mechanic and gives a complete owned/unowned map. **Prerequisite:** get the current, complete ZIP→owner registry from corporate (the owned list Kevin gave is stale). Not guaranteed corporate will let us choose, but this is our preference.
- **Command-central docs** created (this `docs/` set + root `CLAUDE.md`) so context survives thread switches. Lives in the repo/git; external hard-disk copy planned.
- **Auto-seed for every Z, free.** New ELLE tenants get 3 labeled EXAMPLE leads automatically (no API spend); real paid discovery only after confirm. Examples auto-clear on confirm. Window sample-customer seed was NOT built (fake data in prod; ELLE examples already make the app feel alive) — revisit if wanted.
- **Book-A-Truck login gate** shipped: requires a signed-in account before submit (clean/attributable pilot leads); DEMO bypasses. Accepted tradeoff: may reduce cold public bookings later — soften at public launch.
- **Deploy target for pre-tester features = production** (testers use donutnvapp.com), even though design changes otherwise go to staging only.
- **Support email = kevin@donutnv.com** for the pilot (adjustable).

## Earlier (from the build + audit)

- **Square webhook made atomic** (`process_square_sale`) after finding it marked events processed before writing the stamp — a crash could lose or double a loyalty stamp.
- **ELLE API surface locked to service-role**; discovery/enrichment invoked only via the `elle_call_edge_function` dispatcher with spend enforcement.
- **Spend caps + kill switch + alerts** added; LLM cap could never trip because LLM logged nothing, so the dispatcher now estimate-meters non-self-logging paid functions.
- **PWA `selfDestroying: true`** — the service worker was caching a stale/broken prod bundle; self-destruct fixes it.
- **truck_locations retention** — table grew unbounded; nightly prune keeps each truck's latest point (map stays fast via the index regardless).
- **Curtain intro reverted** at root — looked wrong when already logged in.
- Copy rules from Kevin: no em dashes in sent copy; "Make your business sweet!"; ELLE = "E.L.L.E"; leads refreshed "every month"; 5 scoring factors; don't over-claim ownership/paperwork.

## Open decisions (need Kevin / corporate)

- **Customer ownership policy** (ROADMAP #57): is a customer global-to-brand or owned per-location? Re-signup currently overwrites `tenant_id`. Needs a corporate decision before fixing.
- **Rollout geography** — pending the current territory map from corporate.
