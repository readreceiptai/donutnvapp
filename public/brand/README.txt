DROP YOUR EXACT BRAND ART HERE
==============================

Put these files in this folder (public/brand/) and they swap in automatically —
no code changes needed. Until they exist, the app uses on-brand CSS placeholders.

  logo.svg        Your DonutNV logo. Shows in the landing header and the awning.
                  (PNG works too — if you use logo.png instead, tell me and I'll
                  point the <img> at it. SVG is sharpest.)

  awning.png      The striped awning valance across the top of the curtain intro.
                  Wide and short — about 1200 x 240 px, transparent background.
                  Should tile/stretch left-to-right.

  curtain.png     One curtain panel (the app mirrors it for the right side).
                  Tall — about 800 x 1600 px. The blue drapes that slide open.
                  Transparent edges look best.

Optional extras you can add later:
  curtain-left.png / curtain-right.png  — if you want different left/right drapes.
  hero.png        — a truck/food photo for the landing background.

After you add files, just refresh the app (or redeploy). That's it.
The curtain animation, awning shape, and logo placement are already wired —
your art simply fills them in.
